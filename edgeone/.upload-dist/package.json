{
  "name": "pan-search-edgeone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PanSearch EdgeOne 精简版：首页 + 全网搜 + 转存 + 定时清理 + Cookie 设置（Blob 存储）",
  "scripts": {
    "build": "node -e \"console.log('[build] skip — static files already at project root')\"",
    "pack": "node scripts/pack-upload.mjs",
    "build:out": "node scripts/build-public.mjs"
  },
  "dependencies": {
    "@edgeone/pages-blob": "^0.0.14"
  },
  "engines": {
    "node": ">=20"
  }
}
